import { COLORS } from './constants'

const lightTheme = {
	bg: COLORS.text.header,
}

const darkTheme = {
	bg: COLORS.background.dark,
	body: COLORS.text.light,
	primary: COLORS.primary,
}

export { lightTheme, darkTheme }
